#include "lists.h"
/**
 * find_listint_loop - findes the loop in a list
 * @head: head of list
 * Return: address of node loop
 */
listint_t *find_listint_loop(listint_t *head)
{
	listint_t *temp;

	temp = head;

	return (temp);
}
