#include "lists.h"
/**
 * print_listint_safe - prints a linked list safe
 * @head: header of linked
 * Return: number of nodes in list
 */
size_t print_listint_safe(const listint_t *head)
{
	size_t i = 0;
	(void)head;


	return (i);
}
