#include "main.h"

/**
 * mul - Multiplies two integers and returns the result.
 *
 * @a: number one.
 * @b: number two.
 *
 * Return: Multiple of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
